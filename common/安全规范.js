/**
 * 安全规范模块
 * 
 * 【KISS原则文档的一部分】
 * 本文件集中定义项目安全规范，避免文档与代码分离
 * 
 * === 一、API Key 安全 ===
 * 
 * 两级配置架构：
 * 
 * ┌─────────────────────┐      ┌─────────────────────┐
 * │ 管理员配置(官方Key) │      │ 用户配置(个人Key)   │
 * │ system_config 集合  │      │ api_keys 集合       │
 * │ {                   │      │ {                   │
 * │   _id: "api_keys",  │      │   userId: "user_xxx"│
 * │   bailian: {        │      │   bailian: "sk-xxx" │
 * │     apiKey: "sk-官方"│      │   deepseek: "sk-yyy"│
 * │   }                 │      │ }                   │
 * │ }                   │      │                     │
 * └─────────────────────┘      └─────────────────────┘
 *          ↓                            ↓
 *          └──────────┬─────────────────┘
 *                     ↓
 *          ┌─────────────────────┐
 *          │ Key 优先级规则        │
 *          │ 用户Key > 官方Key     │
 *          └─────────────────────┘
 * 
 * Key 优先级：
 * 1. 用户个人 Key（api_keys 集合，按 userId 查询）
 * 2. 官方 Key（system_config 集合）
 * 3. 环境变量（开发调试用，生产环境禁用）
 * 
 * 存储位置：
 * ┌─────────────────┬──────────────────────────┬─────────────────┐
 * │ 集合            │ 存储内容                 │ 权限            │
 * ├─────────────────┼──────────────────────────┼─────────────────┤
 * │ system_config   │ 官方API Key(管理员配置)  │ 仅管理员可写    │
 * │ api_keys        │ 用户个人API Key          │ 用户自己管理    │
 * └─────────────────┴──────────────────────────┴─────────────────┘
 * 
 * 初始化流程：
 * 网关启动时：
 * 1. 检查 system_config 是否有 api_keys 配置
 * 2. 如果没有，从 .env 迁移（首次启动）
 * 3. 迁移后清除 .env 中的明文密钥
 * 
 * CLI 管理：
 * dove config list-keys          # 查看当前配置的 Key
 * dove config set-key bailian sk-xxx   # 设置用户个人 Key
 * dove config test-key bailian  # 测试 Key 是否有效
 * 
 * === 二、文件访问安全 ===
 * 
 * 网关路径安全检查规则：
 * 
 * function sanitizePath(userId, path) {
 *   // 1. 禁止路径穿越
 *   let normalized = path.replace(/\.\./g, '');
 *   
 *   // 2. 禁止绝对路径
 *   if (normalized.startsWith('/')) {
 *     normalized = normalized.slice(1);
 *   }
 *   
 *   // 3. 强制用户目录前缀
 *   return `users/${userId}/${normalized}`;
 * }
 * 
 * 示例：
 * 输入: "../../../etc/passwd"
 * 输出: "users/{userId}/etc/passwd"  // 安全，无法越权
 * 
 * OSS 权限模型：
 * 客户端通过网关：
 * ├── 只能访问 users/{自己的userId}/**
 * ├── 网关强制校验路径
 * └── 防止路径穿越攻击
 * 
 * 鸽群通过 Skill：
 * ├── 可以访问 users/{任务的ownerId}/**
 * ├── 任务关联文件自动继承所有权
 * └── 支持跨用户操作（需任务授权）
 * 
 * === 三、权限控制 ===
 * 
 * 技能权限验证流程：
 * 
 * ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
 * │  网关创建   │     │  任务队列   │     │  技能执行   │
 * │   任务      │     │  存储任务   │     │  权限验证   │
 * └──────┬──────┘     └──────┬──────┘     └──────┬──────┘
 *        │                   │                   │
 *        │ 存储 userRole     │                   │
 *        │─────────────────→│                   │
 *        │                   │                   │
 *        │                   │ 传递 userRole     │
 *        │                   │─────────────────→│
 *        │                   │                   │
 *        │                   │                   │ 检查 requiredRole
 *        │                   │                   │ ↓
 *        │                   │                   │ 权限不足 → 返回错误
 *        │                   │                   │ 权限通过 → 执行技能
 * 
 * 权限级别：
 * ┌─────────────┬────────────┬─────────────────────────────┐
 * │ 角色级别    │ 说明       │ 可执行技能                  │
 * ├─────────────┼────────────┼─────────────────────────────┤
 * │ admin       │ 管理员     │ 所有技能(含需要admin权限的) │
 * │ user        │ 普通用户   │ 仅无权限要求的技能          │
 * │ 其他角色    │ 自定义     │ 对应角色或admin可执行       │
 * └─────────────┴────────────┴─────────────────────────────┘
 * 
 * 技能定义示例：
 * export default {
 *   name: 'model_organize',
 *   description: '使用深度思考模型整理模型配置',
 *   requiredRole: 'admin',  // 需要管理员权限
 *   parameters: { ... },
 *   execute
 * };
 * 
 * 权限验证代码示例：
 * async 执行技能(技能名, 参数, 上下文) {
 *   const 技能 = this.已注册技能.get(技能名);
 *   
 *   // 权限验证
 *   if (技能.requiredRole) {
 *     const 用户角色 = 上下文.userRole || 'user';
 *     if (用户角色 !== 技能.requiredRole && 用户角色 !== 'admin') {
 *       return { 
 *         成功: false, 
 *         错误: `权限不足：需要「${技能.requiredRole}」角色`,
 *         错误码: 'PERMISSION_DENIED'
 *       };
 *     }
 *   }
 *   
 *   return await 技能.execute(参数, 上下文);
 * }
 * 
 * 安全原则：
 * 1. 权限硬编码：权限验证在代码层面强制执行，不可绕过
 * 2. 描述可见：技能描述中明确标注权限要求，LLM 可识别
 * 3. 角色传递：用户角色从网关 → 任务 → 技能执行全程传递
 * 4. 最小权限：默认角色为 user，仅能执行无权限要求的技能
 * 
 * === 四、MongoDB 权限控制 ===
 * 
 * 用户权限模型：
 * ├── 统一使用共享集合（tasks, conversations 等）
 * ├── 通过 userId 索引隔离用户数据
 * ├── 所有查询自动注入 userId 过滤条件
 * └── 禁止跨用户数据访问
 * 
 * === 五、技能数据库权限 ===
 * 
 * 权限验证优先级：
 * 
 * 技能调用请求
 *     │
 *     ├─ 1. 用户是管理员？
 *     │     └─ 是 → 直接通过 ✅
 *     │
 *     ├─ 2. 技能已禁用？
 *     │     └─ 是 → 拒绝（技能不可用）❌
 *     │
 *     ├─ 3. 用户在黑名单？
 *     │     └─ 是 → 拒绝（最高优先级）❌
 *     │
 *     └─ 4. 用户拥有该技能且未过期？
 *           └─ 是 → 通过 ✅
 *           └─ 否 → 拒绝（未授权）❌
 * 
 * === 六、依赖版本锁定 ===
 * 
 * 所有 package.json 中的依赖版本必须使用精确版本号，禁止使用版本范围前缀：
 * 
 * ┌──────────┬─────────────────────┬──────────────────────────────────┐
 * │ 前缀     │ 示例               │ 说明                             │
 * ├──────────┼─────────────────────┼──────────────────────────────────┤
 * │ ❌ ^     │ ^6.0.0             │ 允许兼容更新，构建不可复现       │
 * │ ❌ ~     │ ~6.0.0             │ 允许补丁更新，构建不可复现       │
 * │ ❌ >=    │ >=6.0.0            │ 允许任意更高版本，风险最大       │
 * │ ❌ *     │ *                  │ 任意版本，完全不可控             │
 * │ ✅ 无前缀│ 6.0.0              │ 精确锁定，构建可复现             │
 * └──────────┴─────────────────────┴──────────────────────────────────┘
 * 
 * 原因：
 * 1. 构建可复现性 — 两次 npm install 必须得到完全相同的依赖树
 * 2. 供应链安全 — 防止恶意包通过 semver 兼容更新混入
 * 3. 行为确定性 — semver 兼容的 minor 更新也可能引入行为变化或 bug
 * 4. 审计可追溯 — 确切知道生产环境运行的每个包版本
 */

export const 安全规范 = {
  APIKey: {
    两级配置: true,
    优先级: ['用户Key', '官方Key', '环境变量']
  },
  文件访问: {
    路径安全检查: true,
    用户目录隔离: true
  },
  权限控制: {
    角色级别: ['admin', 'user'],
    硬编码验证: true
  },
  依赖版本: {
    禁止前缀: ['^', '~', '>=', '*', '>', '<'],
    要求精确版本: true
  }
};

export default 安全规范;
