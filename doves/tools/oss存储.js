/**
 * @file tools/oss存储
 * @description 阿里云 OSS 对象存储封装，支持文件上传/下载/删除/签名URL
 */

import { 初始化, 是否可用 } from './oss存储/核心.js';
import { 上传, 上传Base64 } from './oss存储/上传.js';
import { 下载, 删除, 批量删除, 获取签名网址, 检查存在, 列出文件, 复制 } from './oss存储/文件管理.js';
import { 路径常量, 生成用户路径, 生成任务路径, 生成交换路径 } from './oss存储/路径工具.js';
import { 上传截图, 上传任务文件, 测试连接 } from './oss存储/便捷方法.js';

export default {
  // 配置
  初始化,
  是否可用,
  测试连接,
  
  // 核心操作
  上传,
  上传Base64,
  下载,
  删除,
  批量删除,
  复制,
  检查存在,
  列出文件,
  获取签名网址,
  
  // 路径工具
  路径常量,
  生成用户路径,
  生成任务路径,
  生成交换路径,
  
  // 便捷方法
  上传截图,
  上传任务文件
};

