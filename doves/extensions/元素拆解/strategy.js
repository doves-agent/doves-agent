/**
 * 元素拆解规划策略
 * 扩展 = 能力组 + 流程案例
 *
 * 描述可用工具能力 + 提供流程案例参考
 * LLM 根据用户实际请求自行决策工具组合
 */
import { 生成策略提示词, 生成用户提示词 } from '../../prompts/strategy-base.js';

const 方法论段落 = [
  '【元素拆解能力组】',
  '',
  '本扩展提供 3 个原子工具，可独立调用、自由组合：',
  '',
  '1. element_analyze — 识图找元素',
  '   输入图片URL，返回图中所有可拆元素（名称/位置/描述/置信度）',
  '   适用场景：不知道图里有什么、需要元素清单再决定拆哪些',
  '',
  '2. element_extract — 拆解指定元素',
  '   输入图片URL + 元素列表，用万相2.7组图模式拆出元素图片',
  '   支持白底/黑底/透明底三种背景模式，每批最多4个元素',
  '   适用场景：已知要拆哪些元素、直接拆解',
  '',
  '3. element_pack — 打包上传',
  '   将拆解出的元素图片打包为 zip 并上传 OSS，返回下载链接',
  '   适用场景：用户需要批量下载拆解结果',
  '',
  '【流程案例】（参考，非强制）',
  '- 全量拆解：analyze → extract(全部) → pack（可用 element_extract 技能快捷完成）',
  '- 指定拆解：analyze → extract([指定元素])（不需要打包则省略 pack）',
  '- 只分析：analyze（用户只要看图里有什么）',
  '- 改后拆解：[其他工具编辑图片] → extract（跨扩展组合）',
  '',
  '【关键规则】',
  '- 根据用户实际需求灵活组合工具，流程案例仅为参考',
  '- extract 每批最多4个元素，大批量需分批',
  '- extract 的 toolRiskLevel 为 "caution"',
].join('\n');

const 输出格式扩展 = '"extractContext": {\n    "imageUrl": "原图URL或路径",\n    "userPrompt": "用户提示词",\n    "toolChoice": "使用的工具组合说明"\n  },';

const 方法论指引 = '请根据用户实际需求，从元素拆解能力组中选择合适的工具组合。流程案例仅供参考，不必拘泥于固定流程。';

export default {
  strategies: {
    // 策略名 = 技能名（skill name），供 planBranch 技能策略提升使用
    // 意图名→策略别名由 _loader.js 步骤2.5 自动注册，无需手动重复
    element_extract: {
      系统: (最大子任务数 = 10, 当前深度 = 0) => 生成策略提示词(
        '元素拆解',
        方法论段落,
        输出格式扩展,
        最大子任务数,
        当前深度
      ),

      用户: (任务描述, 能力列表, 可用技能 = []) => 生成用户提示词(
        任务描述,
        能力列表,
        可用技能,
        方法论指引
      ),
    },
  },
};
